# Consolidated requirements for all three models in this archive
# (PatchTST/, P_sLSTM/, P_sLSTM_ChannelMixing/). Each subfolder also has its
# own scoped requirements.txt if you only need one model.
torch>=2.2
pandas>=2.2
numpy>=1.26
scikit-learn>=1.3
einops>=0.7
matplotlib>=3.7
